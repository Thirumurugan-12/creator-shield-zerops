import {AppShell} from "../../components/app-shell";import {Card} from "../../components/ui";export default function Coming(){return <AppShell><Card className="mx-auto max-w-xl p-10 text-center"><h1 className="text-xl font-semibold">Coming in the next milestone</h1><p className="mt-2 text-sm text-muted-foreground">This workspace is intentionally not enabled yet. The navigation is ready for the next vertical slice.</p></Card></AppShell>}
