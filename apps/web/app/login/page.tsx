"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {ShieldCheck,ArrowRight,LockKeyhole} from "lucide-react";import Link from "next/link";import {Button,Card} from "../../components/ui";import {demoLogin} from "../../lib/api";import {toast} from "sonner";
export default function Login(){const router=useRouter();const [loading,setLoading]=useState(false);async function enterDemo(){setLoading(true);try{await demoLogin();router.replace("/dashboard")}catch(error){toast.error(error instanceof Error?error.message:"Could not sign in");setLoading(false)}}return <main className="grid min-h-screen place-items-center bg-background px-5"><div className="w-full max-w-md"><Link href="/" className="mx-auto mb-8 flex w-fit items-center gap-2 text-sm font-semibold"><span className="grid size-7 place-items-center rounded-md bg-primary text-primary-foreground"><ShieldCheck size={16}/></span>CreatorShield</Link><Card className="p-7"><div className="mb-7"><div className="mb-4 grid size-10 place-items-center rounded-lg bg-muted text-foreground"><LockKeyhole size={20}/></div><h1 className="text-xl font-semibold">Sign in to your workspace</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Use the demo creator account to explore the secure proof workflow.</p></div><Button className="w-full" onClick={enterDemo} disabled={loading}>{loading?"Starting secure session…":"Continue as demo creator"}<ArrowRight size={16}/></Button><p className="mt-5 text-center text-xs leading-5 text-muted-foreground">Demo access is local to this development environment. Email/password registration will be added in the authentication milestone.</p></Card></div></main>}
